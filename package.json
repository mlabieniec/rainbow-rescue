{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --files \"index.html,style.css,app.js,images/*\"",
    "build": "pwa-asset-generator favicon.png ./images --icon-only -m ./manifest.json -i ./index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.13",
    "pwa-asset-generator": "^3.2.3"
  }
}
